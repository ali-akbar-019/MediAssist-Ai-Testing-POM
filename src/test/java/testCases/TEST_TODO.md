# Selenium Test Todo

## Status

- [x] Create test todo tracker
- [ ] Update frontend pages with stable data-testid selectors
- [ ] Update LoginPage page object to use data-testid selectors
- [ ] Update RegisterPage page object to use data-testid selectors and stable dropdown selection
- [ ] Refactor login tests to validate field errors and real navigation
- [ ] Refactor register tests to validate step 1 and step 2 behavior
- [ ] Add explicit waits in tests where needed
- [ ] Complete the empty feature test classes for other pages
- [ ] Run tests and verify selectors/navigation

## Notes

- Use `data-testid` selectors rather than placeholder text.
- Keep the existing page object pattern intact.
- Use real page routes `/login` and `/register` in tests.
